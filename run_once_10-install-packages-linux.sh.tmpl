{{- if (eq .chezmoi.os "linux") -}}
#!/bin/bash

set -eufo pipefail

repositories=()
packages=(
    git
    curl
    wget
    vim
    htop
    ripgrep
    jq
    zsh
    git-lfs
    tmux
    code
    firefox
    google-chrome
    httpie
)

{{ end }}
